@page
@model AbbyWeb.Pages.Admin.Order.OrderListModel

@{
	var status = HttpContext.Request.Query["status"];
	var cancelled = "text_primary";
	var completed = "text_primary";
	var inProcess = "text_primary";
	var ready = "text_primary";

	if (status == "cancelled")
		cancelled = "active text-white";
	else if (status == "completed")
		completed = "active text-white";
	else if (status == "ready")
		ready = "active text-white";
	else
		inProcess = "active text-white";

}

<div class="container p-3">
	<div class="d-flex justify-content-between">
		<div class="p-2">
			<h2 class="text-primary">Order List</h2>
		</div>
		<div class="p-2 text-end">
			<ul class="list-group list-group-horizontal-sm">
				<a style="text-decoration:none;" asp-page="OrderList" asp-route-status="cancelled">
				<li class="list-group-item @cancelled">Cancelled</li>
				</a>

				<a style="text-decoration:none;" asp-page="OrderList" asp-route-status="completed">
				<li class="list-group-item @completed">Completed</li>
				</a>

				<a style="text-decoration:none;" asp-page="OrderList" asp-route-status="ready">
				<li class="list-group-item @ready">Ready For Pick</li>
				</a>

				<a style="text-decoration:none;" asp-page="OrderList" asp-route-status="inProcess">
				<li class="list-group-item @inProcess">In Process</li>
				</a>
			</ul>
		</div>
	</div>

	<br /><br />

	<table id="DT_load" class="table table-bordered table-striped text-dark" style="width: 100%">
		<thead>
			<tr>
				<th>ORDER ID</th>
				<th>NAME</th>
				<th>EMAIL</th>
				<th>ORDER TOTAL</th>
				<th>PICKUP TIME</th>
				<th></th>
			</tr>
		</thead>
	</table>
</div>

@section Scripts
{
<script src="~/js/orderlist.js"></script>
}
